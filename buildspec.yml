version: 0.2

phases:
  install:
    runtime-versions:
      python: 3.12
    commands:
      - echo "Installing dependencies..."
      - pip install -r requirements.txt -t lib
  build:
    commands:
       - echo "Building the code..." 
  post_build:
    commands:
      - echo "Invoking Lambda function..." 
      - aws lambda invoke --function-name testing_lambda_func --region 'us-east-1'
      - echo "Build completed successfully"